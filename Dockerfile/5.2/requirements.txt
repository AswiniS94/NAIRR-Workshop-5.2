# DriveLM (Case Study 03) — pinned to the versions the notebook targets (env.yml)
transformers==4.38.2
peft==0.9.0
tokenizers==0.15.2
sentencepiece>=0.2.0
accelerate>=0.27.2
safetensors>=0.4.2
huggingface-hub>=0.21.4
# DriveLM evaluation metrics (02_evaluate)
pycocoevalcap==1.2
pycocotools>=2.0.7
# shared / light notebooks (base image already has numpy/pandas/sklearn/matplotlib)
seaborn>=0.13.0
tqdm>=4.65.0
